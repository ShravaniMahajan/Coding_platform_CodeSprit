-- Admin user is seeded programmatically via DataInitializer.java using PasswordEncoder.
-- No SQL insert needed here to avoid hardcoded BCrypt hash issues.
-- Credentials: username=admin, email=admin@codesphere.com, password=admin@123
SELECT 1;
